
<form action="">
    {{ form_start(form)}}

    <div style="margin-top:3em;">
        {{form_row(form.q)}}
    </div>

    {{form_row(form.sexe)}}
    <h4>Age </h4>
    <div class="row">
        <div class="col-md-6">
            {{form_row(form.min)}}
        </div>
        <div class="col-md-6">
            {{form_row(form.max)}}
        </div>
    </div>


    <button type="submit" class="btn btn-primary">filtrer</button>
    {{form_end(form)}}
</form>
